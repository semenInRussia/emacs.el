# -*- mode: snippet -*-
# name: compress values
# key: compr
# --
auto ${2:b} = ${1:a};

sort($2.begin(), $2.end());
$2.resize(unique($2.begin(), $2.end()) - $2.begin());

for (int i = 0; i < n; i++) {
  $1[i] = lower_bound($2.begin(), $2.end(), $1[i]) - $2.begin();
}
